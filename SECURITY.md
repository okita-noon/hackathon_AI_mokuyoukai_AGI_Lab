# セキュリティとデータの扱い

このアプリはハッカソン向けのデモです。ログイン、実課金、個人履歴の外部サービス連携は提供していません。

## 報告方法

APIキー、セッションCookie、個人の入力、悪用に必要な詳細は公開Issueに載せないでください。リポジトリの「Security → Report a vulnerability」が利用できる場合は非公開で報告してください。表示されない場合は、管理者が公開している連絡先から非公開の報告方法を確認してください。

報告には、影響するコミット、再現手順、期待する振る舞いと実際の結果を含めてください。実在の利用者データを使わず、ローカルのテストデータで再現してください。

## 現在の対策

- HttpOnly / SameSite Cookieによる匿名セッション分離（本番ではSecure）
- 保存された約束に対する所有者確認
- 更新時の状態確認とトランザクション
- JSON本文・メディアのサイズ制限とAI出力の実行時検証
- 外部AIのタイムアウト・固定応答への明示的な切り替え
- 非rootコンテナ、基本的なHTTPレスポンスヘッダー
- 実際のお金を動かさないモック罰金

## 残る制約

匿名Cookieはアカウント認証ではありません。共用ブラウザの利用者を識別できず、Cookieを失うと以前の状態へ戻れません。利用量制限・分散レート制限・保存期間に基づく自動削除・費用上限のアプリ内制御は未実装です。公開範囲とAIプロバイダー側の上限を運用側で管理してください。

入力と証拠は、AI設定時に外部AIサービスへ送信されます。AIの説明や判定は誤る可能性があります。機密情報をデモに入力しないでください。固定応答による判定を、実際の達成や本人性の確認として利用しないでください。
