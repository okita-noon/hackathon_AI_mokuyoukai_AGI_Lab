# Local development only. Never commit .env.local or real credentials.
DATABASE_URL=postgresql://commitpay:commitpay@localhost:55432/commitpay_agi_lab

# Leave all AI keys empty for explicitly labelled, deterministic demo responses.
USE_VERTEX=0
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
OPENAI_MODEL=gpt-4o-mini

# Leave empty for mock payments. Use a Stripe TEST key only for local checkout testing.
STRIPE_SECRET_KEY=
APP_BASE_URL=http://localhost:3000
