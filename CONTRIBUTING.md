# 開発に参加する

公開対象は `ai-okan/` です。ルートの `src/` は旧CommitPayのバックエンド実装で、現在の画面ではありません。最初に [README](README.md) の起動手順と [現在の設計](docs/CURRENT_ARCHITECTURE.md) を確認してください。

## 開発の流れ

1. 最新の `main` から作業ブランチを作ります。
2. Node.js 22.9以上、PostgreSQL 16を用意し、READMEの手順でスキーマを適用します。
3. `npm ci --prefix ai-okan` で固定された依存関係をインストールします。
4. 変更した振る舞いをテストに追加し、`npm run check --prefix ai-okan` を実行します。
5. 画面・API・DBの変更は、本番ビルドを起動して `npm run test:e2e --prefix ai-okan` でも確認します。
6. PRには、解決する問題、変更後の振る舞い、実行した検証、残る制約を書きます。

型検査・lint・単体テスト・DBスキーマの再適用・Dockerビルド・ブラウザテストはCIでも実行します。本番デプロイは `main` の検証成功後に実行されます。

## 変更時に守ること

- 匿名セッションごとに所有者を確認し、他の利用者の約束を更新しない。
- AIの応答は信頼できる型として扱わず、実行時の検証を通す。
- AIを呼べなかった場合は、固定応答を実AIの結果として表示しない。
- 罰金はデモ記録のままにする。実課金・第三者送金は別途設計・レビューが必要。
- 入力、証拠、APIキー、Cookieをログ・スクリーンショット・PRに含めない。
- DB変更は再適用可能にし、デプロイ中の旧バージョンとの互換性を保つ。
- 公開情報のデータセットを更新するときは、出典・確認状況・不明な日付を維持する。

`ai-okan/AGENTS.md` は、このNext.jsバージョンのドキュメントを確認するための指示です。自動生成部分を削除しないでください。

## テストと画像の更新

手順、テスト対象、README用画面キャプチャの再生成は [TESTING.md](docs/TESTING.md) にまとめています。ライセンスの追加や第三者素材の再配布条件は、権利者の確認なく決めないでください。
